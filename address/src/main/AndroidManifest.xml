<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ericshenn.address">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".addresslist.AddressListActivity"
            android:label="收货地址" />
        <activity
            android:name=".addressdetail.AddressDetailActivity"
            android:label="地址详情" />

    </application>
</manifest>
